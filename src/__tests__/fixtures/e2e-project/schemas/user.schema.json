{
  "type": "object",
  "properties": {
    "name": { "type": "string", "example": "Autofilled Name" },
    "email": { "type": "string", "format": "email" },
    "role": { "type": "string", "enum": ["admin", "user", "guest"] },
    "age": { "type": "integer", "minimum": 18, "maximum": 99 }
  },
  "required": ["name", "email"]
}
